name: Build

on:
  push:
    branches: [ "main", release/*, story/*, task/* ]
    paths:
      - 'docs-*/**'
      - 'pom.xml'
      - '.github/workflows/**'
  pull_request:
    #    branches: [ "main" ]
    types: [opened, synchronize, reopened]
    paths:
      - 'docs-*/**'
      - 'pom.xml'
  workflow_dispatch:

env:
  API_DOCKER_REGISTRY: ghcr.io
  API_DOCKER_IMAGE_NAME: ${{ github.repository_owner }}/docs-func-aws-template-list-v1

permissions:
  pull-requests: read # allows SonarCloud to decorate PRs with analysis results

jobs:
  build_job:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Git checkout
        uses: actions/checkout@v3

      - name: Install node v18
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Rebuild local npm repository from cache
        uses: actions/cache@v3
        with:
          path: |
            ~/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Yarn install
        run: yarn install

      - name: Yarn build
        run: yarn build

  test_job:
    name: Run tests
    needs: build_job
    runs-on: ubuntu-latest
    environment: ci

    steps:
      - name: Git checkout
        uses: actions/checkout@v3

      - name: Rebuild local npm repository from cache
        uses: actions/cache@v3
        with:
          path: |
            ~/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Yarn install
        run: yarn install

      - name: Build project
        run: yarn build

      - name: Run tests
        env:
          repository.template.provider.aws.s3.bucketname: ${{ env.TEST_AWS_BUCKETNAME }}
          repository.template.provider.aws.s3.region: ${{ env.TEST_AWS_REGION }}
          repository.template.provider.aws.s3.prefix: ${{ env.TEST_AWS_BUCKET_PREFIX }}

          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: yarn test

  analyze_job:
    name: Code analysis
    needs:
      - build_job
      - test_job
    runs-on: ubuntu-latest
    environment: ci

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - name: Rebuild local npm repository from cache
        uses: actions/cache@v3
        with:
          path: |
            ~/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Yarn install
        run: yarn install

      - name: Build project
        run: yarn build

      - name: Run tests
        env:
          repository.template.provider.aws.s3.bucketname: ${{ env.TEST_AWS_BUCKETNAME }}
          repository.template.provider.aws.s3.region: ${{ env.TEST_AWS_REGION }}
          repository.template.provider.aws.s3.prefix: ${{ env.TEST_AWS_BUCKET_PREFIX }}

          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: yarn test -- --coverage

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets. SONARCLOUD_TOKEN }}

  docker_build_job:
    name: Containerize
    needs:
      - analyze_job
    permissions:
      contents: read # for actions/checkout to fetch code
      security-events: write # for github/codeql-action/upload-sarif to upload SARIF results
      actions: read # only required for a private repository by gitHub/codeql-action/upload-sarif to get the Action run status
      packages: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - name: Rebuild local npm repository from cache
        uses: actions/cache@v3
        with:
          path: |
            ~/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Yarn install
        run: yarn install

      - name: Build project
        run: yarn build

#      - name: Build the Docker image
#        working-directory: ./docs-generator-api
#        run: docker build -t ${{ env.API_DOCKER_REGISTRY }}/${{ env.API_DOCKER_IMAGE_NAME }} --file Dockerfile .

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to the Container registry
        uses: docker/login-action@f054a8b539a109f9f41c372932f1ae047eff08c9
        with:
          registry: ${{ env.API_DOCKER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v2
        with:
          images: ${{ env.API_DOCKER_REGISTRY }}/${{ env.API_DOCKER_IMAGE_NAME }}
          tags: |
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./docs-generator-api
          push: true
#          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Pull back the Docker image for the Trivy scan
        working-directory: ./docs-generator-api
        run: docker pull ${{ steps.meta.outputs.tags }}

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@7b7aa264d83dc58691451798b4d117d53d21edfe
        with:
          image-ref: '${{ steps.meta.outputs.tags }}'
          format: 'template'
          template: '@/contrib/sarif.tpl'
          output: 'trivy-results.sarif'
          severity: 'CRITICAL,HIGH'

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: 'trivy-results.sarif'

